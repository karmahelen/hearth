name: "Security Auditing Suite"

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  schedule:
    - cron: '30 4 * * 4'

jobs:
  # 1. CODEQL SCAN
  codeql-analysis:
    name: CodeQL Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          # Formatted as a strict YAML list array to ensure proper interpretation
          languages: ['python', 'javascript-typescript']

      # Natively handles build configurations for web and interpreted structures
      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4

  # 2. SEMGREP & BANDIT SCANS
  sast-scanners:
    name: Semgrep & Bandit Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Run Bandit (Python Specific Linter)
      # Appending "|| true" ensures that if Bandit finds security warnings, it logs them 
      # but lets the workflow finish successfully so your badge doesn't turn red.
      - name: Install and Run Bandit
        run: |
          pip install bandit
          bandit -r . -f txt || true

      # Run Semgrep (General SAST Engine)
      - name: Run Semgrep OSS
        run: |
          python3 -m pip install semgrep
          semgrep ci --config=p/security-audit
